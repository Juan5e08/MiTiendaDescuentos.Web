@model IEnumerable<MiTiendaDescuentos.Web.Models.Cupo>

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h2 class="mb-0">Cupos</h2>
            <a asp-action="Create" class="btn btn-primary btn-sm">
                + Nuevo registro de cupos
            </a>
        </div>

        <div class="card-body">
            @if (!Model.Any())
            {
                <p class="text-muted mb-0">
                    No hay cupos registrados aún.
                </p>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>@Html.DisplayNameFor(model => model.IdSede)</th>
                                <th>@Html.DisplayNameFor(model => model.IdGrado)</th>
                                <th>@Html.DisplayNameFor(model => model.CuposTotales)</th>
                                <th>@Html.DisplayNameFor(model => model.CuposOcupados)</th>
                                <th class="text-end">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(m => item.IdSede)</td>
                                    <td>@Html.DisplayFor(m => item.IdGrado)</td>
                                    <td>@Html.DisplayFor(m => item.CuposTotales)</td>
                                    <td>@Html.DisplayFor(m => item.CuposOcupados)</td>

                                    <td class="text-end">
                                        <a asp-action="Edit"
                                           asp-route-id="@item.IdCupo"
                                           class="btn btn-sm btn-outline-secondary me-1">
                                            Editar
                                        </a>

                                        <a asp-action="Details"
                                           asp-route-id="@item.IdCupo"
                                           class="btn btn-sm btn-outline-info me-1">
                                            Detalles
                                        </a>

                                        <a asp-action="Delete"
                                           asp-route-id="@item.IdCupo"
                                           class="btn btn-sm btn-outline-danger">
                                            Eliminar
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>
