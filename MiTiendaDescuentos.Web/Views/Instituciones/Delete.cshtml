@model MiTiendaDescuentos.Web.Models.Institucion

@{
    ViewData["Title"] = "Eliminar institución";
}

<div class="container mt-4">
    <h1 class="mb-3">Eliminar institución</h1>

    <!-- Mensajes de error (por ejemplo, institución con sedes asociadas) -->
    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

    <p class="text-muted">
        ¿Estás seguro de que deseas eliminar esta institución? Esta acción no se puede deshacer.
        <br />
        <small>
            Nota: si la institución tiene sedes asociadas, no se podrá eliminar hasta eliminar primero esas sedes.
        </small>
    </p>

    <div class="card mb-3">
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Nombre)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Nombre)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Localidad)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Localidad)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.DireccionPrincipal)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.DireccionPrincipal)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Correo)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Correo)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Telefono)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Telefono)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.CantidadSedes)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.CantidadSedes)</dd>
            </dl>
        </div>
    </div>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="IdColegio" />
        <button type="submit" class="btn btn-danger">Eliminar</button>
        <a asp-action="Index" class="btn btn-secondary ms-2">Volver al listado</a>
    </form>
</div>
